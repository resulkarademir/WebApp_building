@model List<WebApp_building.DAL.Entities.Referanslar>
@{
    Layout = "~/Views/Layout/Index.cshtml";
}

<div class="main-panel">
    <div class="content">
        <div class="container-fluid">
            <h4 class="page-title">Referanslar</h4>
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            <div class="card-title">ReferanslarList</div>
                        </div>
                        <div class="card-body">
                            <div class="card-sub">
                                You can perform Referanslar-related operations below:
                            </div>

                            <!-- Scrollable tablo kapsayıcısı -->
                            <div class="table-responsive">
                                <table class="table mt-3 table-bordered table-striped align-middle">
                                    <thead class="table-light">
                                        <tr>
                                            <th>ID</th>
                                            <th>Title</th>
                                            <th>Category</th>
                                            <th>Image</th>
                                            <th>Description</th>
                                            <th>Delete</th>
                                            <th>Update</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>@item.Id</td>
                                                <td>@item.Title</td>
                                                <td>@item.Category</td>
                                                <td>@item.Image</td>
                                                <td>@item.Description</td>


                                                <td>
                                                    <a class="btn btn-danger btn-sm" href="/Referanslar/ReferansSil/@item.Id">Delete</a>
                                                </td>
                                                <td>
                                                    <a class="btn btn-success btn-sm" href="/Referanslar/ReferansGuncelle/@item.Id">Update</a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>

                            <a href="/Referanslar/ReferansEkle" class="btn btn-info mt-3">Add New Referanslar</a>
                        </div>

                    </div>

                </div>

            </div>
        </div>
    </div>

</div>